apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-infer-cal.fullname" . }}
  labels:
    {{- include "llm-infer-cal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-infer-cal.selectorLabels" . | nindent 4 }}
